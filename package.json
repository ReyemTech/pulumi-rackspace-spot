{
  "name": "pulumi-rackspace-spot-root",
  "private": true,
  "workspaces": [
    "provider",
    "sdk"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^4.1.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
